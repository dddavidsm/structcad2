<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>StructCAD Pro — Inspección Estructural</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet"/>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{font-size:14px}
:root{
  --w:#fff;--g50:#f8f9fa;--g100:#f1f3f5;--g200:#e9ecef;--g300:#dee2e6;
  --g400:#ced4da;--g500:#adb5bd;--g600:#6c757d;--g700:#495057;
  --g800:#343a40;--g900:#212529;
  --b50:#e7f0ff;--b100:#c3d9ff;--b200:#90b8ff;--b500:#2563eb;--b600:#1d4ed8;
  --r50:#fff0f0;--r500:#ef4444;
  --orange:#f97316;--green:#16a34a;
  --font:'Inter',system-ui,sans-serif;--mono:'IBM Plex Mono',monospace;
  --sh:0 1px 3px rgba(0,0,0,.1),0 1px 2px rgba(0,0,0,.06);
  --sh2:0 4px 6px rgba(0,0,0,.07);
  --r:5px;--rl:10px;--sb:260px;--tb:52px;
}
body{font-family:var(--font);background:var(--g100);color:var(--g900);min-height:100vh;line-height:1.5}

/* TOPBAR */
.tb{position:fixed;top:0;left:0;right:0;height:var(--tb);background:var(--w);
  border-bottom:1px solid var(--g200);display:flex;align-items:center;
  padding:0 1.25rem;gap:.75rem;z-index:300;box-shadow:var(--sh)}
.tb-logo{display:flex;align-items:center;gap:.55rem}
.tb-mark{width:30px;height:30px;background:var(--b500);border-radius:var(--r);
  display:flex;align-items:center;justify-content:center;flex-shrink:0}
.tb-mark svg{width:17px;height:17px;fill:white}
.tb-name{font-size:.92rem;font-weight:700;color:var(--g900)}
.tb-name b{color:var(--b500)}
.tb-sep{width:1px;height:20px;background:var(--g200)}
.tb-sub{font-size:.75rem;color:var(--g600);font-weight:500}
.tb-sp{flex:1}
.tb-ver{font-size:.65rem;background:var(--b50);color:var(--b600);
  border:1px solid var(--b100);padding:.18rem .55rem;border-radius:100px;font-weight:500}

/* SIDEBAR */
.sb{position:fixed;top:var(--tb);left:0;bottom:0;width:var(--sb);
  background:var(--w);border-right:1px solid var(--g200);overflow-y:auto;
  z-index:200;padding:.6rem 0}
.sb-sec{margin-bottom:.2rem}
.sb-lbl{font-size:.62rem;font-weight:600;letter-spacing:.1em;text-transform:uppercase;
  color:var(--g500);padding:.5rem 1rem .3rem}
.sb-a{display:flex;align-items:center;gap:.55rem;padding:.5rem 1rem;
  cursor:pointer;font-size:.8rem;font-weight:500;color:var(--g700);
  border-left:3px solid transparent;transition:all .12s}
.sb-a:hover{background:var(--g50);color:var(--g900)}
.sb-a.on{background:var(--b50);color:var(--b500);border-left-color:var(--b500)}
.sb-a svg{width:15px;height:15px;flex-shrink:0;opacity:.65}
.sb-a.on svg{opacity:1}
.sb-n{margin-left:auto;font-size:.62rem;background:var(--g200);
  color:var(--g600);padding:.1rem .4rem;border-radius:100px}
.sb-a.on .sb-n{background:var(--b100);color:var(--b600)}

/* MAIN */
.main{margin-left:var(--sb);padding-top:var(--tb)}
.pg{display:none;padding:1.25rem}.pg.on{display:block}

/* PAGE HEADER */
.ph{display:flex;align-items:flex-start;justify-content:space-between;
  margin-bottom:1.25rem;gap:1rem;flex-wrap:wrap}
.ph h1{font-size:1.15rem;font-weight:700;margin-bottom:.15rem}
.ph p{font-size:.77rem;color:var(--g600)}
.ph-btns{display:flex;gap:.45rem;flex-wrap:wrap}

/* BUTTONS */
.btn{display:inline-flex;align-items:center;gap:.35rem;font-family:var(--font);
  font-size:.77rem;font-weight:500;padding:.42rem .85rem;border-radius:var(--r);
  cursor:pointer;border:1px solid transparent;transition:all .13s;white-space:nowrap}
.btn svg{width:13px;height:13px}
.bp{background:var(--b500);color:#fff;border-color:var(--b500)}
.bp:hover{background:var(--b600)}
.bs{background:var(--w);color:var(--g700);border-color:var(--g300)}
.bs:hover{background:var(--g50);border-color:var(--g400)}
.btn:disabled{opacity:.45;cursor:not-allowed}
.btn-lg{font-size:.84rem;padding:.55rem 1.1rem}

/* CARD */
.card{background:var(--w);border:1px solid var(--g200);border-radius:var(--rl);box-shadow:var(--sh)}
.ch{padding:.8rem 1.1rem;border-bottom:1px solid var(--g200);
  display:flex;align-items:center;justify-content:space-between;gap:.65rem}
.ch h3{font-size:.85rem;font-weight:600}
.ch p{font-size:.72rem;color:var(--g500);margin-top:.08rem}
.cb{padding:1.1rem}
.cf{padding:.65rem 1.1rem;border-top:1px solid var(--g200);
  background:var(--g50);border-radius:0 0 var(--rl) var(--rl);
  display:flex;align-items:center;gap:.65rem}

/* STRUCT SELECTOR */
.sg{display:grid;grid-template-columns:repeat(auto-fit,minmax(148px,1fr));
  gap:.65rem;margin-bottom:1.25rem}
.sc{background:var(--w);border:2px solid var(--g200);border-radius:var(--rl);
  padding:.9rem;cursor:pointer;transition:all .13s;text-align:center}
.sc:hover{border-color:var(--b500);box-shadow:var(--sh2)}
.sc.on{border-color:var(--b500);background:var(--b50);
  box-shadow:0 0 0 3px rgba(37,99,235,.1)}
.sc-ico{width:44px;height:44px;margin:0 auto .55rem;background:var(--g100);
  border-radius:var(--r);display:flex;align-items:center;justify-content:center}
.sc.on .sc-ico{background:var(--b100)}
.sc-ico svg{width:26px;height:26px}
.sc h4{font-size:.78rem;font-weight:600;margin-bottom:.15rem}
.sc p{font-size:.67rem;color:var(--g500);line-height:1.35}

/* FORM */
.fl{display:grid;grid-template-columns:1fr 1.5fr;gap:1.1rem;align-items:start}
@media(max-width:1100px){.fl{grid-template-columns:1fr}}
.fs{margin-bottom:1.1rem}
.fst{font-size:.68rem;font-weight:600;letter-spacing:.07em;text-transform:uppercase;
  color:var(--g500);margin-bottom:.65rem;padding-bottom:.4rem;
  border-bottom:1px solid var(--g200);display:flex;align-items:center;gap:.35rem}
.fg{display:grid;grid-template-columns:repeat(auto-fit,minmax(120px,1fr));gap:.55rem}
.f{display:flex;flex-direction:column;gap:.28rem}
.f label{font-size:.7rem;font-weight:500;color:var(--g700)}
.f label .u{font-weight:400;color:var(--g500);font-size:.65rem}
.f input,.f select,.f textarea{
  background:var(--w);border:1px solid var(--g300);border-radius:var(--r);
  color:var(--g900);font-family:var(--font);font-size:.8rem;padding:.38rem .6rem;
  width:100%;transition:border-color .13s,box-shadow .13s}
.f input:focus,.f select:focus,.f textarea:focus{
  outline:none;border-color:var(--b500);box-shadow:0 0 0 3px rgba(37,99,235,.08)}
.f textarea{resize:vertical;min-height:60px;font-size:.75rem}

/* TABS */
.tabs{display:flex;border-bottom:1px solid var(--g200);margin-bottom:1.1rem;gap:.05rem}
.tab{font-size:.77rem;font-weight:500;color:var(--g600);padding:.5rem .8rem;
  cursor:pointer;border-bottom:2px solid transparent;margin-bottom:-1px;
  transition:all .12s;display:flex;align-items:center;gap:.35rem}
.tab:hover{color:var(--g900)}
.tab.on{color:var(--b500);border-bottom-color:var(--b500)}
.tab svg{width:12px;height:12px}

/* STATUS */
.sdot{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.sdot.ok{background:var(--green)}.sdot.err{background:var(--r500)}
.sdot.idle{background:var(--g400)}.sdot.spin{background:var(--orange);animation:pulse .8s infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.35}}
.stxt{font-size:.73rem;color:var(--g600)}

/* STEPS */
.steps{display:flex;align-items:center;gap:.4rem;margin-bottom:1.25rem;flex-wrap:wrap}
.step{display:flex;align-items:center;gap:.35rem;font-size:.72rem;font-weight:500;color:var(--g400)}
.step.on{color:var(--b500)}.step.done{color:var(--green)}
.sn{width:20px;height:20px;border-radius:50%;border:2px solid currentColor;
  display:flex;align-items:center;justify-content:center;font-size:.6rem;font-weight:700;flex-shrink:0}
.step.done .sn{background:var(--green);border-color:var(--green);color:#fff}
.step.on .sn{background:var(--b500);border-color:var(--b500);color:#fff}
.step-sep{flex:1;height:1px;background:var(--g200);min-width:16px;max-width:40px}

/* ══ CANVAS SYSTEM ══ */
.cv-wrap{display:flex;flex-direction:column;gap:.65rem}
.cv-toolbar{display:flex;align-items:center;gap:.35rem;padding:.5rem .7rem;
  background:var(--w);border:1px solid var(--g200);border-radius:var(--r);flex-wrap:wrap}
.cv-sep{width:1px;height:18px;background:var(--g200);margin:0 .1rem}
.cv-label{font-size:.62rem;font-weight:600;color:var(--g500);text-transform:uppercase;letter-spacing:.07em}
.tool{display:inline-flex;align-items:center;gap:.28rem;font-size:.7rem;font-weight:500;
  color:var(--g600);background:var(--g50);border:1px solid var(--g200);border-radius:var(--r);
  padding:.26rem .5rem;cursor:pointer;transition:all .1s;user-select:none}
.tool:hover{background:var(--g100);color:var(--g800)}
.tool.on{background:var(--b50);color:var(--b600);border-color:var(--b200)}
.tool svg{width:12px;height:12px}
.bs-row{display:flex;align-items:center;gap:.28rem}
.bs-dot{width:22px;height:22px;border-radius:50%;display:flex;align-items:center;
  justify-content:center;cursor:pointer;border:2px solid transparent;
  background:var(--g50);transition:all .1s}
.bs-dot.on{border-color:var(--b500)}.bs-dot:hover{background:var(--g100)}
.bs-fill{border-radius:50%;background:var(--g700)}

/* Canvas layers container */
.cv-container{position:relative;border:1px solid var(--g200);border-radius:var(--r);
  overflow:hidden;
  background-color:#f0ede8;
  background-image:
    linear-gradient(rgba(0,0,0,.035) 1px,transparent 1px),
    linear-gradient(90deg,rgba(0,0,0,.035) 1px,transparent 1px);
  background-size:20px 20px;
  cursor:crosshair;
}
#cvBase,#cvPick,#cvBars,#cvOver{position:absolute;top:0;left:0;display:block}
#cvBase{pointer-events:none}
#cvBars{pointer-events:none}
#cvPick{cursor:crosshair}
#cvOver{cursor:crosshair}
.cv-info{position:absolute;bottom:5px;right:7px;background:rgba(255,255,255,.92);
  border:1px solid var(--g200);border-radius:var(--r);padding:.22rem .5rem;
  font-size:.62rem;color:var(--g600);font-family:var(--mono);pointer-events:none}
.cv-legend{display:flex;align-items:center;gap:.75rem;flex-wrap:wrap;
  padding:.42rem .65rem;background:var(--g50);border:1px solid var(--g200);
  border-radius:var(--r);font-size:.68rem;color:var(--g600)}
.cv-leg{display:flex;align-items:center;gap:.28rem}
.cv-swatch{width:12px;height:12px;border-radius:2px;flex-shrink:0}

/* Bar status panel */
.bar-status-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(68px,1fr));gap:.38rem;margin-top:.5rem}
.bar-stat{background:var(--g50);border:1.5px solid var(--g200);border-radius:var(--r);
  padding:.42rem .35rem;text-align:center;cursor:pointer;transition:all .13s;user-select:none}
.bar-stat:hover{border-color:var(--g400)}
.bar-stat.found{background:#dcfce7;border-color:var(--green);color:#166534}
.bar-stat.notfound{background:var(--r50);border-color:#fca5a5;color:#991b1b}
.bar-stat .bs-label{font-size:.65rem;font-weight:600}
.bar-stat .bs-pos{font-size:.58rem;opacity:.7;font-family:var(--mono)}

/* Crack list */
.crack-list{display:flex;flex-direction:column;gap:.28rem;margin-top:.35rem;max-height:100px;overflow-y:auto}
.crack-item{display:flex;align-items:center;gap:.45rem;background:var(--r50);
  border:1px solid #fca5a5;border-radius:var(--r);padding:.28rem .5rem;font-size:.7rem;color:#991b1b}
.crack-del{background:none;border:none;cursor:pointer;color:#fca5a5;margin-left:auto;font-size:.85rem;padding:0 .2rem}
.crack-del:hover{color:var(--r500)}

/* Upload */
.upload-z{border:2px dashed var(--g300);border-radius:var(--rl);padding:1.25rem;
  text-align:center;cursor:pointer;transition:all .13s;background:var(--g50)}
.upload-z:hover,.upload-z.drag{border-color:var(--b500);background:var(--b50)}
.upload-z svg{width:26px;height:26px;color:var(--g400);margin:0 auto .5rem;display:block}
.upload-z p{font-size:.77rem;color:var(--g600)}
.upload-z span{font-size:.67rem;color:var(--g500)}

/* Table */
.dt{width:100%;border-collapse:collapse;font-size:.76rem}
.dt th{background:var(--g50);color:var(--g600);font-weight:600;padding:.42rem .65rem;
  text-align:left;border-bottom:1px solid var(--g200);font-size:.67rem;
  letter-spacing:.04em;text-transform:uppercase}
.dt td{padding:.48rem .65rem;border-bottom:1px solid var(--g100);color:var(--g800)}
.dt tr:hover td{background:var(--g50)}
.mono{font-family:var(--mono);font-size:.7rem}

/* Settings */
.sr{display:flex;align-items:center;justify-content:space-between;
  padding:.62rem 0;border-bottom:1px solid var(--g100)}
.sr:last-child{border:none}
.sr-lbl{font-size:.8rem;font-weight:500;color:var(--g800)}
.sr-desc{font-size:.68rem;color:var(--g500);margin-top:.1rem}
.si{background:var(--w);border:1px solid var(--g300);border-radius:var(--r);
  color:var(--g900);font-family:var(--mono);font-size:.75rem;padding:.3rem .5rem;min-width:160px}
.si:focus{outline:none;border-color:var(--b500)}
.tgl{position:relative;display:inline-block;width:34px;height:18px}
.tgl input{opacity:0;width:0;height:0}
.tgl-tr{position:absolute;inset:0;background:var(--g300);border-radius:9px;cursor:pointer;transition:background .2s}
.tgl-th{position:absolute;width:14px;height:14px;background:#fff;border-radius:50%;top:2px;left:2px;transition:transform .2s;pointer-events:none;box-shadow:var(--sh)}
.tgl input:checked~.tgl-tr{background:var(--b500)}
.tgl input:checked~.tgl-th{transform:translateX(16px)}

/* Tag */
.tag{font-size:.65rem;font-weight:500;padding:.18rem .5rem;border-radius:100px;display:inline-flex;align-items:center;gap:.25rem}
.tg-blue{background:var(--b50);color:var(--b600);border:1px solid var(--b100)}
.tg-gray{background:var(--g100);color:var(--g600);border:1px solid var(--g300)}
.tg-green{background:#dcfce7;color:#166534;border:1px solid #86efac}
</style>
</head>
<body>

<!-- TOPBAR -->
<div class="tb">
  <div class="tb-logo">
    <div class="tb-mark"><svg viewBox="0 0 18 18"><path d="M2 16V6l7-4 7 4v10H2z"/><rect x="6" y="10" width="6" height="6" fill="rgba(0,0,0,.3)"/></svg></div>
    <div class="tb-name">Struct<b>CAD</b> Pro</div>
  </div>
  <div class="tb-sep"></div>
  <div class="tb-sub">Inspección Estructural</div>
  <div class="tb-sp"></div>
  <div class="tb-ver">v2.1 — DXF R2000</div>
</div>

<!-- SIDEBAR -->
<nav class="sb">
  <div class="sb-sec">
    <div class="sb-lbl">Principal</div>
    <a class="sb-a on" onclick="nav('nueva',this)">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="12" height="12" rx="2"/><line x1="8" y1="5" x2="8" y2="11"/><line x1="5" y1="8" x2="11" y2="8"/></svg>Nueva Inspección
    </a>
  </div>
  <div class="sb-sec">
    <div class="sb-lbl">Gestión</div>
    <a class="sb-a" onclick="nav('historial',this)">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M2 4h12M2 8h8M2 12h5"/></svg>Historial
      <span class="sb-n" id="hcount">0</span>
    </a>
    <a class="sb-a" onclick="nav('obra',this)">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="8" width="10" height="6" rx="1"/><path d="M1 8h14M6 8V5a2 2 0 014 0v3"/></svg>Datos de Obra
    </a>
  </div>
  <div class="sb-sec">
    <div class="sb-lbl">Sistema</div>
    <a class="sb-a" onclick="nav('config',this)">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="8" cy="8" r="2.5"/><path d="M8 2v1.5M8 12.5V14M2 8h1.5M12.5 8H14M4.1 4.1l1 1M10.9 10.9l1 1M10.9 4.1l1-1M4.1 10.9l1-1"/></svg>Configuración
    </a>
  </div>
</nav>

<!-- MAIN -->
<main class="main">

<!-- ═══ NUEVA INSPECCIÓN ═══ -->
<div class="pg on" id="pg-nueva">
<div class="ph">
  <div><h1>Nueva Inspección</h1>
  <p>Las barras y estribos se auto-posicionan desde los parámetros. Pinte libremente la zona picada.</p></div>
  <div class="ph-btns">
    <button class="btn bs" onclick="resetAll()">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M2 8A6 6 0 0113.8 5.5M14 8a6 6 0 01-11.8 2.5"/><polyline points="14 3 14 6 11 6"/></svg>Limpiar
    </button>
    <button class="btn bp btn-lg" id="btnDXF" onclick="generarDXF()">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M8 2v8M5 7l3 3 3-3M3 13h10"/></svg>Generar DXF
    </button>
  </div>
</div>

<div class="steps">
  <div class="step on" id="s1"><div class="sn">1</div>Tipo</div><div class="step-sep"></div>
  <div class="step" id="s2"><div class="sn">2</div>Parámetros</div><div class="step-sep"></div>
  <div class="step" id="s3"><div class="sn">3</div>Zona picada</div><div class="step-sep"></div>
  <div class="step" id="s4"><div class="sn">4</div>Exportar DXF</div>
</div>

<!-- Selector -->
<div class="sg" id="sg">
  <div class="sc" onclick="selS('pilar-rect')" id="sc-pilar-rect">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><rect x="4" y="3" width="20" height="22" rx="1"/><rect x="7" y="6" width="14" height="16" stroke-dasharray="3 2"/><circle cx="7" cy="6" r="1.5" fill="currentColor"/><circle cx="14" cy="6" r="1.5" fill="currentColor"/><circle cx="21" cy="6" r="1.5" fill="currentColor"/><circle cx="7" cy="22" r="1.5" fill="currentColor"/><circle cx="14" cy="22" r="1.5" fill="currentColor"/><circle cx="21" cy="22" r="1.5" fill="currentColor"/></svg></div>
    <h4>Pilar Rectangular</h4><p>3 vistas: planta + alzados</p>
  </div>
  <div class="sc" onclick="selS('pilar-circ')" id="sc-pilar-circ">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><circle cx="14" cy="14" r="10"/><circle cx="14" cy="14" r="6.5" stroke-dasharray="3 2"/><circle cx="14" cy="7.5" r="1.5" fill="currentColor"/><circle cx="20.5" cy="10.5" r="1.5" fill="currentColor"/><circle cx="20.5" cy="17.5" r="1.5" fill="currentColor"/><circle cx="14" cy="20.5" r="1.5" fill="currentColor"/><circle cx="7.5" cy="17.5" r="1.5" fill="currentColor"/><circle cx="7.5" cy="10.5" r="1.5" fill="currentColor"/></svg></div>
    <h4>Pilar Circular</h4><p>Sección + alzado</p>
  </div>
  <div class="sc" onclick="selS('viga')" id="sc-viga">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><rect x="2" y="9" width="24" height="11" rx="1"/><line x1="5" y1="11.5" x2="23" y2="11.5" stroke-width="2"/><line x1="5" y1="17.5" x2="23" y2="17.5" stroke-dasharray="2 1.5"/><line x1="9" y1="10" x2="9" y2="19"/><line x1="15" y1="10" x2="15" y2="19"/><line x1="21" y1="10" x2="21" y2="19"/></svg></div>
    <h4>Viga</h4><p>Sección transversal + alzado</p>
  </div>
  <div class="sc" onclick="selS('forjado')" id="sc-forjado">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><rect x="2" y="9" width="24" height="8" rx="1"/><line x1="7" y1="9" x2="7" y2="17"/><line x1="12" y1="9" x2="12" y2="17"/><line x1="17" y1="9" x2="17" y2="17"/><line x1="22" y1="9" x2="22" y2="17"/><line x1="2" y1="13" x2="26" y2="13" stroke-dasharray="3 2"/></svg></div>
    <h4>Forjado / Losa</h4><p>Planta armadura + sección</p>
  </div>
  <div class="sc" onclick="selS('zapata')" id="sc-zapata">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><rect x="2" y="17" width="24" height="8" rx="1"/><line x1="6" y1="17" x2="6" y2="25"/><line x1="11" y1="17" x2="11" y2="25"/><line x1="17" y1="17" x2="17" y2="25"/><line x1="22" y1="17" x2="22" y2="25"/><line x1="2" y1="20.5" x2="26" y2="20.5"/><rect x="10" y="5" width="8" height="13" stroke-dasharray="2 1.5"/></svg></div>
    <h4>Zapata Aislada</h4><p>Planta + secciones X-X / Y-Y</p>
  </div>
  <div class="sc" onclick="selS('escalera')" id="sc-escalera">
    <div class="sc-ico"><svg viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.3"><line x1="4" y1="24" x2="4" y2="18"/><line x1="4" y1="18" x2="11" y2="18"/><line x1="11" y1="18" x2="11" y2="12"/><line x1="11" y1="12" x2="18" y2="12"/><line x1="18" y1="12" x2="18" y2="6"/><line x1="18" y1="6" x2="25" y2="6"/><path d="M4 24L25 24" stroke-dasharray="2 1.5"/></svg></div>
    <h4>Escalera / Zanca</h4><p>Sección longitudinal inclinada</p>
  </div>
</div>

<!-- Formulario + Canvas -->
<div id="fa" style="display:none">
<div class="fl">

  <!-- FORMULARIO -->
  <div>
    <div class="card" id="fcard">
      <div class="ch">
        <div><h3 id="ftitle">Parámetros</h3><p>Todos los campos calculan el plano en tiempo real</p></div>
        <span class="tag tg-blue" id="ftag">—</span>
      </div>
      <div class="cb">
        <div class="tabs" id="ftabs"></div>
        <div id="fpanels"></div>
      </div>
      <div class="cf">
        <div class="sdot idle" id="gdot"></div>
        <div class="stxt" id="gstxt">Preparado para generar</div>
      </div>
    </div>
  </div>

  <!-- CANVAS + HERRAMIENTAS -->
  <div class="cv-wrap">

    <!-- Toolbar -->
    <div class="cv-toolbar">
      <span class="cv-label">Zona picada</span>
      <button class="tool on" id="t-pick" onclick="setTool('pick')" title="Brocha libre para pintar zona picada">
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M3 11L9 2l2 2-6 9H3v-2z"/></svg>Brocha
      </button>
      <button class="tool" id="t-erase" onclick="setTool('erase')" title="Borrar zona pintada">
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M10 3L4 9l-2 2h4l6-6-2-2z"/></svg>Borrar
      </button>
      <div class="cv-sep"></div>
      <span class="cv-label">Tamaño</span>
      <div class="bs-row">
        <div class="bs-dot on" id="b-s" onclick="setBrush(8)"><div class="bs-fill" style="width:4px;height:4px"></div></div>
        <div class="bs-dot" id="b-m" onclick="setBrush(18)"><div class="bs-fill" style="width:8px;height:8px"></div></div>
        <div class="bs-dot" id="b-l" onclick="setBrush(30)"><div class="bs-fill" style="width:13px;height:13px"></div></div>
      </div>
      <div class="cv-sep"></div>
      <span class="cv-label">Fisuras</span>
      <button class="tool" id="t-crack" onclick="setTool('crack')">
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M3 2l2 4-3 2 4 4"/></svg>Dibujar
      </button>
      <div class="cv-sep"></div>
      <button class="tool" onclick="undoCV()" title="Deshacer (zona picada)">
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M2 7A5 5 0 117 2"/><polyline points="2 3 2 7 6 7"/></svg>
      </button>
      <button class="tool" onclick="clearCV()" title="Limpiar todo" style="color:var(--r500)">
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><line x1="2" y1="2" x2="12" y2="12"/><line x1="12" y1="2" x2="2" y2="12"/></svg>
      </button>
    </div>

    <!-- Canvas multicapa -->
    <div class="cv-container" id="cvCont" style="height:380px">
      <canvas id="cvBase"></canvas>
      <canvas id="cvPick"
        onmousedown="cvDown(event)" onmousemove="cvMove(event)"
        onmouseup="cvUp()" onmouseleave="cvUp()"
        ontouchstart="cvTS(event)" ontouchmove="cvTM(event)" ontouchend="cvUp()">
      </canvas>
      <canvas id="cvBars"></canvas>
      <canvas id="cvOver"
        onmousedown="cvDown(event)" onmousemove="cvMove(event)"
        onmouseup="cvUp()" onmouseleave="cvUp()"
        ontouchstart="cvTS(event)" ontouchmove="cvTM(event)" ontouchend="cvUp()">
      </canvas>
      <div class="cv-info" id="cvInfo">Seleccione una estructura</div>
    </div>

    <!-- Leyenda -->
    <div class="cv-legend">
      <div class="cv-leg"><div class="cv-swatch" style="background:rgba(251,146,60,.5);border:1px solid #ea580c"></div>Zona picada (libre)</div>
      <div class="cv-leg"><div class="cv-swatch" style="background:#1e40af;border-radius:50%"></div>Barra (auto-posicionada)</div>
      <div class="cv-leg"><div class="cv-swatch" style="background:#15803d"></div>Estribo (auto)</div>
      <div class="cv-leg"><div class="cv-swatch" style="background:#dc2626"></div>Fisura</div>
    </div>

    <!-- Panel estado de barras -->
    <div class="card">
      <div class="ch">
        <div><h3>Estado de armadura</h3><p>Clic en cada barra para marcar encontrada / no encontrada</p></div>
        <span class="tag tg-gray" id="barSumTag">— barras</span>
      </div>
      <div class="cb" style="padding:.75rem">
        <div id="barStatusGrid" class="bar-status-grid">
          <div style="font-size:.73rem;color:var(--g500);grid-column:1/-1;text-align:center;padding:.5rem">
            Aparecerán al configurar los parámetros de armadura
          </div>
        </div>
        <div id="crackPanel" style="margin-top:.7rem;display:none">
          <div style="font-size:.68rem;font-weight:600;color:var(--g500);text-transform:uppercase;letter-spacing:.07em;margin-bottom:.35rem">Fisuras registradas</div>
          <div class="crack-list" id="crackList"></div>
        </div>
      </div>
    </div>

    <!-- Foto -->
    <div class="card">
      <div class="ch"><h3>Fotografía de obra</h3><span style="font-size:.68rem;color:var(--g500)">Opcional</span></div>
      <div class="cb">
        <div class="upload-z" id="upz" onclick="document.getElementById('upInput').click()"
          ondragover="event.preventDefault();this.classList.add('drag')"
          ondrop="doDrop(event)" ondragleave="this.classList.remove('drag')">
          <svg viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="2" y="6" width="28" height="20" rx="3"/><circle cx="21" cy="13" r="3"/><path d="M2 24l8-8 5 5 5-6 8 9"/></svg>
          <p>Arrastrar o <strong>clic</strong> para cargar foto</p>
          <span>JPG, PNG — máx. 10 MB</span>
        </div>
        <input type="file" id="upInput" accept="image/*" style="display:none" onchange="loadPh(this)"/>
        <div id="phPrev" style="display:none;margin-top:.65rem">
          <img id="phImg" style="max-width:100%;border-radius:var(--r);border:1px solid var(--g200)"/>
          <button class="btn bs" style="margin-top:.4rem;font-size:.7rem" onclick="clearPh()">Eliminar foto</button>
        </div>
      </div>
    </div>

  </div><!-- /cv-wrap -->
</div><!-- /fl -->
</div><!-- /fa -->
</div><!-- /pg-nueva -->

<!-- ═══ HISTORIAL ═══ -->
<div class="pg" id="pg-historial">
<div class="ph">
  <div><h1>Historial</h1><p>Inspecciones generadas en esta sesión</p></div>
  <button class="btn bs" onclick="exportCSV()">
    <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M8 2v8M5 7l3 3 3-3M3 13h10"/></svg>Exportar CSV
  </button>
</div>
<div class="card">
  <div class="ch"><h3>Registros</h3><span class="tag tg-blue" id="totalReg">0 registros</span></div>
  <div style="overflow-x:auto">
    <table class="dt">
      <thead><tr><th>Ref.</th><th>Tipo</th><th>Fecha</th><th>Sección</th><th>Armadura</th><th>Barras found.</th><th>Fisuras</th><th></th></tr></thead>
      <tbody id="histBody"><tr><td colspan="8" style="text-align:center;color:var(--g500);padding:1.5rem">Sin registros</td></tr></tbody>
    </table>
  </div>
</div>
</div>

<!-- ═══ DATOS DE OBRA ═══ -->
<div class="pg" id="pg-obra">
<div class="ph">
  <div><h1>Datos de Obra</h1><p>Se incluyen automáticamente en el cajetín de todos los planos DXF</p></div>
  <button class="btn bp" onclick="saveObra()">Guardar datos</button>
</div>
<div style="display:grid;grid-template-columns:1fr 1fr;gap:1.1rem">
  <div class="card">
    <div class="ch"><h3>Proyecto</h3></div>
    <div class="cb">
      <div style="display:flex;flex-direction:column;gap:.6rem">
        <div class="f"><label>Nombre de la obra</label><input type="text" id="o-nombre" placeholder="Edificio Residencial, C/ Mayor 14"/></div>
        <div class="f"><label>Código de obra</label><input type="text" id="o-codigo" placeholder="OB-2024-0032"/></div>
        <div class="f"><label>Promotor / Cliente</label><input type="text" id="o-promotor"/></div>
        <div class="f"><label>Dirección</label><input type="text" id="o-dir"/></div>
      </div>
    </div>
  </div>
  <div class="card">
    <div class="ch"><h3>Técnicos</h3></div>
    <div class="cb">
      <div style="display:flex;flex-direction:column;gap:.6rem">
        <div class="f"><label>Técnico responsable</label><input type="text" id="o-tecnico"/></div>
        <div class="f"><label>Nº colegiado</label><input type="text" id="o-colegiado"/></div>
        <div class="f"><label>Empresa inspectora</label><input type="text" id="o-empresa" placeholder="StructCAD Ingeniería"/></div>
        <div class="f"><label>Fecha inspección</label><input type="date" id="o-fecha"/></div>
        <div class="f"><label>Normativa</label>
          <select id="o-normativa"><option>EHE-08</option><option>EC2 — Eurocódigo 2</option><option>ACI 318</option></select>
        </div>
      </div>
    </div>
  </div>
</div>
</div>

<!-- ═══ CONFIGURACIÓN ═══ -->
<div class="pg" id="pg-config">
<div class="ph"><div><h1>Configuración</h1><p>Conexión al backend y preferencias</p></div></div>
<div style="display:grid;grid-template-columns:1fr 1fr;gap:1.1rem">
  <div class="card">
    <div class="ch"><h3>Conexión API</h3></div>
    <div class="cb">
      <div class="sr">
        <div><div class="sr-lbl">URL del servidor</div><div class="sr-desc">FastAPI local o remoto</div></div>
        <input type="text" class="si" id="apiUrl" value="http://localhost:8000"/>
      </div>
      <div class="sr" style="padding-top:.8rem">
        <div style="display:flex;align-items:center;gap:.55rem">
          <div class="sdot idle" id="apiDot"></div>
          <span class="stxt" id="apiTxt">No verificado</span>
        </div>
        <button class="btn bs" onclick="checkApi()">Verificar conexión</button>
      </div>
    </div>
  </div>
  <div class="card">
    <div class="ch"><h3>DXF</h3></div>
    <div class="cb">
      <div class="sr">
        <div><div class="sr-lbl">Escala del plano</div></div>
        <select class="si" id="cfg-esc"><option>1:20</option><option>1:25</option><option>1:50</option></select>
      </div>
      <div class="sr">
        <div><div class="sr-lbl">Guardar en historial</div></div>
        <label class="tgl"><input type="checkbox" id="cfg-hist" checked/><div class="tgl-tr"></div><div class="tgl-th"></div></label>
      </div>
    </div>
  </div>
</div>
</div>

</main>

<script>
/* ══════════════════════════════════════════════════════════
   ESTADO GLOBAL
══════════════════════════════════════════════════════════ */
const S={
  struct:null, tool:'pick', brush:8, drawing:false,
  crackPts:null, history:[], cvHistory:[], cracks:[],
  barStatus:{}, barPositions:[]
};

/* ══ NAV ══ */
function nav(id,el){
  document.querySelectorAll('.pg').forEach(p=>p.classList.remove('on'));
  document.querySelectorAll('.sb-a').forEach(a=>a.classList.remove('on'));
  document.getElementById('pg-'+id)?.classList.add('on');
  if(el)el.classList.add('on');
}

/* ══ DEFINICIÓN DE ESTRUCTURAS ══ */
const STRUCTS={
  'pilar-rect':{
    label:'Pilar Rectangular', endpoint:'/generate/pillar-rect',
    tabs:{
      geometria:[{s:'Sección transversal',f:[
        {id:'width', l:'Ancho frontal',u:'cm',t:'n',mn:15,mx:300,st:1,v:88},
        {id:'depth', l:'Canto lateral',u:'cm',t:'n',mn:15,mx:300,st:1,v:68},
      ]}],
      armadura:[
        {s:'Cara frontal',f:[
          {id:'bars_front_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:16,st:1,v:5},
          {id:'bars_front_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:20},
          {id:'cover_front',l:'Recubrimiento',u:'cm',t:'n',mn:1,mx:12,st:.5,v:5},
        ]},
        {s:'Cara lateral',f:[
          {id:'bars_lateral_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:16,st:1,v:4},
          {id:'bars_lateral_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:20},
          {id:'cover_lateral',l:'Recubrimiento',u:'cm',t:'n',mn:1,mx:12,st:.5,v:6},
        ]},
        {s:'Estribos',f:[
          {id:'stirrup_diam',l:'Ø estribo',u:'mm',t:'n',mn:4,mx:20,st:2,v:6},
          {id:'stirrup_spacing',l:'Separación',u:'cm',t:'n',mn:5,mx:50,st:5,v:15},
        ]},
      ],
      inspeccion:[{s:'Zona inspeccionada',f:[
        {id:'inspection_height',l:'Altura zona picada',u:'cm',t:'n',mn:5,mx:150,st:1,v:25},
        {id:'rebar_found',l:'Armadura encontrada',t:'s',opts:['Sí','No','Parcialmente'],v:'Sí'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:5},
        {id:'corrosion',l:'Estado armadura',t:'s',opts:['Sin patologías','Corrosión leve','Corrosión severa','Armadura seccionada'],v:'Sin patologías'},
        {id:'notes',l:'Notas técnicas',t:'ta',v:''},
        {id:'anomalies',l:'Anomalías detectadas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'P-01'},
        {id:'planta',l:'Planta / Nivel',t:'tx',v:'PB'},
        {id:'eje',l:'Eje / Alineación',t:'tx',v:'A-3'},
        {id:'fecha_insp',l:'Fecha inspección',t:'d',v:''},
      ]}],
    }
  },
  'pilar-circ':{
    label:'Pilar Circular', endpoint:'/generate/pillar-circ',
    tabs:{
      geometria:[{s:'Sección',f:[{id:'diameter',l:'Diámetro',u:'cm',t:'n',mn:20,mx:300,st:1,v:50}]}],
      armadura:[{s:'Armadura',f:[
        {id:'bars_count',l:'Nº barras',u:'ud',t:'n',mn:4,mx:16,st:1,v:8},
        {id:'bars_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:20},
        {id:'cover',l:'Recubrimiento',u:'cm',t:'n',mn:1,mx:12,st:.5,v:4},
        {id:'stirrup_diam',l:'Ø espiral',u:'mm',t:'n',mn:4,mx:20,st:2,v:8},
        {id:'stirrup_spacing',l:'Paso',u:'cm',t:'n',mn:5,mx:30,st:5,v:10},
      ]}],
      inspeccion:[{s:'Inspección',f:[
        {id:'inspection_height',l:'Altura zona picada',u:'cm',t:'n',mn:5,mx:150,st:1,v:25},
        {id:'rebar_found',l:'Armadura',t:'s',opts:['Sí','No','Parcialmente'],v:'Sí'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:4},
        {id:'notes',l:'Notas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'PC-01'},
        {id:'planta',l:'Planta',t:'tx',v:'PB'},
        {id:'eje',l:'Eje',t:'tx',v:'B-2'},
        {id:'fecha_insp',l:'Fecha',t:'d',v:''},
      ]}],
    }
  },
  'viga':{
    label:'Viga', endpoint:'/generate/beam',
    tabs:{
      geometria:[{s:'Sección',f:[
        {id:'width',l:'Ancho',u:'cm',t:'n',mn:15,mx:150,st:1,v:30},
        {id:'height',l:'Canto',u:'cm',t:'n',mn:20,mx:300,st:1,v:60},
        {id:'span',l:'Luz total',u:'cm',t:'n',mn:100,mx:2000,st:10,v:600},
      ]}],
      armadura:[
        {s:'Armadura inferior',f:[
          {id:'bars_bottom_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:10,st:1,v:4},
          {id:'bars_bottom_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:20},
        ]},
        {s:'Armadura superior',f:[
          {id:'bars_top_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:6,st:1,v:2},
          {id:'bars_top_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:16},
        ]},
        {s:'Estribos',f:[
          {id:'cover',l:'Recubrimiento',u:'cm',t:'n',mn:1,mx:10,st:.5,v:3},
          {id:'stirrup_diam',l:'Ø estribo',u:'mm',t:'n',mn:4,mx:20,st:2,v:8},
          {id:'stirrup_spacing',l:'Separación',u:'cm',t:'n',mn:5,mx:50,st:5,v:15},
        ]},
      ],
      inspeccion:[{s:'Inspección',f:[
        {id:'inspection_length',l:'Longitud inspeccionada',u:'cm',t:'n',mn:10,mx:300,st:5,v:60},
        {id:'rebar_found',l:'Armadura',t:'s',opts:['Sí','No','Parcialmente'],v:'Sí'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:3},
        {id:'corrosion',l:'Estado',t:'s',opts:['Sin patologías','Corrosión leve','Corrosión severa'],v:'Sin patologías'},
        {id:'notes',l:'Notas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'V-01'},
        {id:'planta',l:'Planta',t:'tx',v:'P1'},
        {id:'eje',l:'Eje',t:'tx',v:'1-2/A'},
        {id:'fecha_insp',l:'Fecha',t:'d',v:''},
      ]}],
    }
  },
  'forjado':{
    label:'Forjado / Losa', endpoint:'/generate/forjado',
    tabs:{
      geometria:[{s:'Dimensiones',f:[
        {id:'thickness',l:'Canto total',u:'cm',t:'n',mn:10,mx:60,st:1,v:25},
        {id:'span_x',l:'Luz dir. X',u:'cm',t:'n',mn:100,mx:1500,st:10,v:500},
        {id:'span_y',l:'Luz dir. Y',u:'cm',t:'n',mn:100,mx:1500,st:10,v:400},
        {id:'forjado_type',l:'Tipo',t:'s',opts:['Losa maciza','Losa aligerada','Forjado reticular'],v:'Losa maciza'},
      ]}],
      armadura:[
        {s:'Dir. X (paralela al vano principal)',f:[
          {id:'bars_x_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:30,st:1,v:10},
          {id:'bars_x_diam',l:'Ø',u:'mm',t:'n',mn:6,mx:32,st:2,v:12},
          {id:'bars_x_spacing',l:'Separación',u:'cm',t:'n',mn:5,mx:30,st:5,v:15},
        ]},
        {s:'Dir. Y (perpendicular)',f:[
          {id:'bars_y_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:30,st:1,v:10},
          {id:'bars_y_diam',l:'Ø',u:'mm',t:'n',mn:6,mx:32,st:2,v:12},
          {id:'bars_y_spacing',l:'Separación',u:'cm',t:'n',mn:5,mx:30,st:5,v:15},
        ]},
        {s:'Recubrimientos',f:[
          {id:'cover_bottom',l:'Inferior',u:'cm',t:'n',mn:2,mx:10,st:.5,v:3},
          {id:'cover_top',l:'Superior',u:'cm',t:'n',mn:2,mx:10,st:.5,v:3},
        ]},
      ],
      inspeccion:[{s:'Inspección',f:[
        {id:'inspection_area',l:'Área inspeccionada',u:'cm²',t:'n',mn:50,mx:5000,st:50,v:400},
        {id:'rebar_found',l:'Armadura',t:'s',opts:['Sí','No','Parcialmente'],v:'Sí'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:3},
        {id:'notes',l:'Notas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'F-01'},
        {id:'planta',l:'Planta',t:'tx',v:'P1'},
        {id:'eje',l:'Paño',t:'tx',v:'A3-B4'},
        {id:'fecha_insp',l:'Fecha',t:'d',v:''},
      ]}],
    }
  },
  'zapata':{
    label:'Zapata Aislada', endpoint:'/generate/footing',
    tabs:{
      geometria:[{s:'Dimensiones',f:[
        {id:'length',l:'Longitud',u:'cm',t:'n',mn:50,mx:600,st:5,v:200},
        {id:'width',l:'Anchura',u:'cm',t:'n',mn:50,mx:600,st:5,v:160},
        {id:'height',l:'Canto',u:'cm',t:'n',mn:30,mx:200,st:5,v:60},
        {id:'pedestal_w',l:'Pilar ancho',u:'cm',t:'n',mn:20,mx:100,st:5,v:40},
        {id:'pedestal_d',l:'Pilar canto',u:'cm',t:'n',mn:20,mx:100,st:5,v:40},
      ]}],
      armadura:[
        {s:'Armadura dir. X',f:[
          {id:'bars_x_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:20,st:1,v:8},
          {id:'bars_x_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:16},
        ]},
        {s:'Armadura dir. Y',f:[
          {id:'bars_y_count',l:'Nº barras',u:'ud',t:'n',mn:2,mx:20,st:1,v:7},
          {id:'bars_y_diam',l:'Ø barras',u:'mm',t:'n',mn:6,mx:40,st:2,v:16},
        ]},
        {s:'Recubrimientos',f:[
          {id:'cover_bottom',l:'Inferior',u:'cm',t:'n',mn:3,mx:15,st:.5,v:7},
          {id:'cover_sides',l:'Lateral',u:'cm',t:'n',mn:3,mx:15,st:.5,v:7},
        ]},
      ],
      inspeccion:[{s:'Inspección',f:[
        {id:'rebar_found',l:'Armadura',t:'s',opts:['Sí','No','Parcialmente'],v:'Sí'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:7},
        {id:'notes',l:'Notas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'Z-01'},
        {id:'planta',l:'Nivel',t:'tx',v:'Cimentación'},
        {id:'eje',l:'Pilar asociado',t:'tx',v:'P-01'},
        {id:'fecha_insp',l:'Fecha',t:'d',v:''},
      ]}],
    }
  },
  'escalera':{
    label:'Escalera / Zanca', endpoint:'/generate/stair',
    tabs:{
      geometria:[{s:'Geometría',f:[
        {id:'stair_width',l:'Ancho escalera',u:'cm',t:'n',mn:80,mx:300,st:5,v:120},
        {id:'riser',l:'Contrahuella',u:'cm',t:'n',mn:14,mx:22,st:.5,v:17},
        {id:'tread',l:'Huella',u:'cm',t:'n',mn:25,mx:35,st:.5,v:28},
        {id:'slab_thickness',l:'Canto zanca',u:'cm',t:'n',mn:10,mx:30,st:1,v:15},
        {id:'wall_thickness',l:'Espesor pared peldaño',u:'cm',t:'n',mn:5,mx:15,st:.5,v:6.5},
        {id:'steps_count',l:'Nº peldaños',u:'ud',t:'n',mn:3,mx:15,st:1,v:5},
      ]}],
      armadura:[{s:'Armadura',f:[
        {id:'bars_long_diam',l:'Ø barras long.',u:'mm',t:'n',mn:6,mx:20,st:2,v:12},
        {id:'bars_long_sep',l:'Separación long.',u:'cm',t:'n',mn:10,mx:30,st:5,v:15},
        {id:'bars_trans_diam',l:'Ø barras trans.',u:'mm',t:'n',mn:6,mx:16,st:2,v:8},
        {id:'bars_trans_sep',l:'Separación trans.',u:'cm',t:'n',mn:10,mx:30,st:5,v:20},
        {id:'cover',l:'Recubrimiento',u:'cm',t:'n',mn:1.5,mx:6,st:.5,v:2.5},
      ]}],
      inspeccion:[{s:'Inspección',f:[
        {id:'rebar_found',l:'Armadura encontrada',t:'s',opts:['Sí','No','Parcialmente'],v:'No'},
        {id:'cover_measured',l:'Recubrimiento medido',u:'cm',t:'n',mn:0,mx:20,st:.5,v:2.5},
        {id:'relleno_type',l:'Tipo relleno peldaño',t:'s',opts:['Mortero/Cascote','Hormigón','Sin datos'],v:'Mortero/Cascote'},
        {id:'depth_no_rebar',l:'Prof. sin armadura',u:'cm',t:'n',mn:0,mx:30,st:.5,v:10},
        {id:'notes',l:'Notas',t:'ta',v:''},
      ]}],
      obra:[{s:'Identificación',f:[
        {id:'element_id',l:'Referencia',t:'tx',v:'ESC-01'},
        {id:'planta',l:'Tramo',t:'tx',v:'PB-P1'},
        {id:'fecha_insp',l:'Fecha',t:'d',v:''},
      ]}],
    }
  }
};

/* ══ SELECCIÓN ══ */
let curTab='geometria';
function selS(id){
  S.struct=id;
  document.querySelectorAll('.sc').forEach(c=>c.classList.remove('on'));
  document.getElementById('sc-'+id)?.classList.add('on');
  const def=STRUCTS[id];
  document.getElementById('ftitle').textContent=def.label;
  document.getElementById('ftag').textContent=def.label;
  document.getElementById('fa').style.display='block';
  curTab='geometria';
  renderTabs(id);
  setStep(2);
  setTimeout(()=>{renderCanvasTemplate();updateBarStatusPanel();},60);
}

/* ══ TABS + FORM ══ */
const tabNames={geometria:'Geometría',armadura:'Armadura',inspeccion:'Inspección',obra:'Obra'};
const tabIcons={
  geometria:'<svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="1" y="1" width="12" height="12" rx="1"/></svg>',
  armadura:'<svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><line x1="2" y1="4" x2="12" y2="4"/><line x1="2" y1="7" x2="12" y2="7"/><line x1="2" y1="10" x2="12" y2="10"/></svg>',
  inspeccion:'<svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="7" cy="7" r="5"/><path d="M7 4v3l2 1.5"/></svg>',
  obra:'<svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M2 12V6l5-4 5 4v6"/><rect x="5" y="8" width="4" height="4"/></svg>'
};

function renderTabs(id){
  const def=STRUCTS[id];
  document.getElementById('ftabs').innerHTML=Object.keys(def.tabs).map(k=>
    `<div class="tab${k===curTab?' on':''}" onclick="swTab('${k}')">${tabIcons[k]}${tabNames[k]}</div>`
  ).join('');
  renderPanel(id,curTab);
}
function swTab(t){
  curTab=t;renderTabs(S.struct);
  if(t==='geometria'||t==='armadura')setTimeout(()=>{renderCanvasTemplate();updateBarStatusPanel();},60);
}
function renderPanel(id,tab){
  const secs=STRUCTS[id].tabs[tab]||[];
  document.getElementById('fpanels').innerHTML=secs.map(sec=>
    `<div class="fs"><div class="fst">${sec.s}</div><div class="fg">${sec.f.map(rf).join('')}</div></div>`
  ).join('');
  document.querySelectorAll('input[type=date]').forEach(d=>{if(!d.value)d.value=new Date().toISOString().split('T')[0];});
  document.querySelectorAll('#fpanels input[type=number]').forEach(el=>{
    el.addEventListener('input',()=>setTimeout(()=>{renderCanvasTemplate();updateBarStatusPanel();},80));
  });
}
function rf(f){
  const u=f.u?`<span class="u">(${f.u})</span>`:'';
  if(f.t==='ta')return`<div class="f" style="grid-column:1/-1"><label>${f.l}</label><textarea id="${f.id}">${f.v||''}</textarea></div>`;
  if(f.t==='s')return`<div class="f"><label>${f.l}</label><select id="${f.id}">${(f.opts||[]).map(o=>`<option${o===f.v?' selected':''}>${o}</option>`).join('')}</select></div>`;
  if(f.t==='d')return`<div class="f"><label>${f.l}</label><input type="date" id="${f.id}" value="${f.v||''}"/></div>`;
  if(f.t==='tx')return`<div class="f"><label>${f.l} ${u}</label><input type="text" id="${f.id}" value="${f.v||''}"/></div>`;
  return`<div class="f"><label>${f.l} ${u}</label><input type="number" id="${f.id}" min="${f.mn??''}" max="${f.mx??''}" step="${f.st||1}" value="${f.v??''}"/></div>`;
}

function getP(){
  if(!S.struct)return{};
  const p={};
  // Read all panels from all tabs
  const def=STRUCTS[S.struct];
  for(const tab of Object.values(def.tabs))for(const sec of tab)for(const f of sec.f){
    const el=document.getElementById(f.id);
    if(el) p[f.id]=el.type==='number'?parseFloat(el.value)||0:el.value;
    else p[f.id]=f.t==='n'?f.v||0:f.v||'';
  }
  p.obra_nombre=document.getElementById('o-nombre')?.value||'';
  p.tecnico=document.getElementById('o-tecnico')?.value||'';
  return p;
}

/* ══════════════════════════════════════════════════
   CANVAS MULTICAPA
   
   cvBase  — plantilla estructural (auto-generada, no tocar)
   cvPick  — zona picada (usuario pinta libremente)
   cvBars  — barras y estribos AUTO-posicionados (no tocar)
   cvOver  — fisuras del usuario
══════════════════════════════════════════════════ */
const cvBase=document.getElementById('cvBase');
const cvPick=document.getElementById('cvPick');
const cvBars=document.getElementById('cvBars');
const cvOver=document.getElementById('cvOver');
const ctxB=cvBase.getContext('2d');
const ctxP=cvPick.getContext('2d');
const ctxBr=cvBars.getContext('2d');
const ctxO=cvOver.getContext('2d');

function resizeCV(){
  const cont=document.getElementById('cvCont');
  const W=cont.clientWidth, H=cont.clientHeight||380;
  [cvBase,cvPick,cvBars,cvOver].forEach(c=>{
    c.width=W;c.height=H;c.style.width=W+'px';c.style.height=H+'px';
  });
}

/* ── Render capa base ── */
function renderCanvasTemplate(){
  if(!S.struct)return;
  resizeCV();
  const W=cvBase.width,H=cvBase.height;
  ctxB.clearRect(0,0,W,H);ctxBr.clearRect(0,0,W,H);
  S.barPositions=[];
  const p=getP();
  if(S.struct==='pilar-rect') drawPilarRect(p,W,H);
  else if(S.struct==='pilar-circ') drawPilarCirc(p,W,H);
  else if(S.struct==='viga') drawViga(p,W,H);
  else if(S.struct==='forjado') drawForjado(p,W,H);
  else if(S.struct==='zapata') drawZapata(p,W,H);
  else if(S.struct==='escalera') drawEscalera(p,W,H);
  drawBarsLayer();
  document.getElementById('cvInfo').textContent=`${STRUCTS[S.struct].label} — escala visual`;
  setStep(3);
}

/* Textura hormigón intacto */
function fillConcrete(ctx,x,y,w,h){
  ctx.fillStyle='#c4c0b8';ctx.fillRect(x,y,w,h);
  const rng=lcg(9876);
  ctx.fillStyle='#a8a49c';
  for(let i=0;i<Math.min(w*h/15,180);i++){
    ctx.beginPath();ctx.arc(x+rng()*w,y+rng()*h,rng()*1.6+.3,0,Math.PI*2);ctx.fill();
  }
}
/* Textura zona picada (áridos visibles) */
function fillExposed(ctx,x,y,w,h){
  ctx.fillStyle='#e8e4dc';ctx.fillRect(x,y,w,h);
  const rng=lcg(1234);
  for(let i=0;i<Math.min(w*h/18,150);i++){
    ctx.fillStyle=`rgba(160,155,145,${.3+rng()*.5})`;
    ctx.save();ctx.translate(x+rng()*w,y+rng()*h);ctx.rotate(rng()*Math.PI);
    const s=rng()*3+1;ctx.fillRect(-s,-s*.6,s*2,s*1.2);ctx.restore();
  }
}
function lcg(seed){let s=seed;return()=>{s=Math.imul(s,1664525)+1013904223;return((s>>>0)/4294967296);};}

/* ── Pilar rectangular ── */
function drawPilarRect(p,W,H){
  const w=p.width||88,d=p.depth||68;
  const cf=p.cover_front||5,cl=p.cover_lateral||6;
  const nbf=p.bars_front_count||5,nbl=p.bars_lateral_count||4;
  const df=p.bars_front_diam||20,dl=p.bars_lateral_diam||20,ds=p.stirrup_diam||6;
  const sc=Math.min(W*.52/w,H*.68/d);
  const ox=(W-w*sc)/2,oy=(H-d*sc)/2;

  // Hormigón intacto
  fillConcrete(ctxB,ox,oy,w*sc,d*sc);

  // Zona picada (parte inferior-izquierda, forma orgánica)
  ctxB.save();
  ctxB.beginPath();
  ctxB.moveTo(ox,oy+d*sc*.42);
  ctxB.bezierCurveTo(ox+w*sc*.15,oy+d*sc*.55, ox+w*sc*.35,oy+d*sc*.62, ox+w*sc*.62,oy+d*sc*.82);
  ctxB.bezierCurveTo(ox+w*sc*.72,oy+d*sc*.9, ox+w*sc*.65,oy+d*sc, ox+w*sc*.65,oy+d*sc);
  ctxB.lineTo(ox,oy+d*sc);ctxB.closePath();
  ctxB.clip();
  fillExposed(ctxB,ox,oy,w*sc,d*sc);
  ctxB.restore();

  // Borde zona picada
  ctxB.beginPath();
  ctxB.moveTo(ox,oy+d*sc*.42);
  ctxB.bezierCurveTo(ox+w*sc*.15,oy+d*sc*.55,ox+w*sc*.35,oy+d*sc*.62,ox+w*sc*.62,oy+d*sc*.82);
  ctxB.bezierCurveTo(ox+w*sc*.72,oy+d*sc*.9,ox+w*sc*.65,oy+d*sc,ox+w*sc*.65,oy+d*sc);
  ctxB.strokeStyle='#7c6d5a';ctxB.lineWidth=1;ctxB.setLineDash([3,2]);ctxB.stroke();ctxB.setLineDash([]);

  // Contorno exterior grueso
  ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2.5;ctxB.strokeRect(ox,oy,w*sc,d*sc);

  // Estribo (verde oscuro, grosor proporcional)
  const ds_px=Math.max(1.2,ds/20*sc*0.3);
  ctxB.strokeStyle='#155e27';ctxB.lineWidth=ds_px;
  const ex=ox+cf*sc,ey=oy+cl*sc,ew=w*sc-2*cf*sc,eh=d*sc-2*cl*sc;
  rrect(ctxB,ex,ey,ew,eh,3);

  // Líneas de cota técnicas
  ctxB.strokeStyle='#495057';ctxB.lineWidth=.6;ctxB.setLineDash([]);
  // Cota horizontal total
  dimH(ctxB,ox,ox+w*sc,oy-4,`${w}`);
  // Cota vertical total
  dimV(ctxB,oy,oy+d*sc,ox+w*sc+4,`${d}`);
  // Sub-cotas barras frontales
  const spf=(w-2*cf)/(nbf-1||1);
  const spl=(d-2*cl)/(nbl-1||1);
  ctxB.fillStyle='#6c757d';ctxB.font=`400 9px ${monoFont}`;ctxB.textAlign='center';
  // Labels sub-cotas
  ctxB.fillText(`${cf}`,ox+cf*sc/2,oy-2);
  for(let i=0;i<nbf-1;i++)ctxB.fillText(`${spf.toFixed(0)}`,ox+(cf+i*spf+spf/2)*sc,oy-2);
  ctxB.fillText(`${cf}`,ox+w*sc-cf*sc/2,oy-2);

  // Barras frontales (cara inferior = oy+cf*sc)
  for(let i=0;i<nbf;i++){
    const bx=ox+(cf+i*spf)*sc,by=oy+cf*sc;
    const r=Math.max(3,df/20*sc*.52);
    S.barPositions.push({id:`F${i+1}`,label:`F${i+1}`,cx:bx,cy:by,r,diam:df,type:'frontal',found:S.barStatus[`F${i+1}`]||'unknown'});
  }
  // Barras laterales (cara izquierda = ox+cl*sc)
  for(let i=0;i<nbl;i++){
    const bx=ox+cl*sc,by=oy+(cl+i*spl)*sc;
    const r=Math.max(3,dl/20*sc*.52);
    S.barPositions.push({id:`L${i+1}`,label:`L${i+1}`,cx:bx,cy:by,r,diam:dl,type:'lateral',found:S.barStatus[`L${i+1}`]||'unknown'});
  }
}

/* ── Pilar circular ── */
function drawPilarCirc(p,W,H){
  const diam=p.diameter||50,R=diam/2,cov=p.cover||4,nb=p.bars_count||8,db=p.bars_diam||20;
  const sc=Math.min(W*.5/diam,H*.65/diam);
  const cx=W/2,cy=H/2;
  fillConcrete(ctxB,cx-R*sc,cy-R*sc,diam*sc,diam*sc);
  ctxB.save();ctxB.beginPath();ctxB.arc(cx,cy,R*sc,0,Math.PI*2);ctxB.clip();
  fillConcrete(ctxB,cx-R*sc,cy-R*sc,diam*sc,diam*sc);ctxB.restore();
  // Zona picada inferior
  ctxB.save();ctxB.beginPath();ctxB.arc(cx,cy,R*sc,0,Math.PI*2);ctxB.clip();
  ctxB.save();ctxB.beginPath();ctxB.arc(cx,cy+R*sc*.2,R*sc*.8,0,Math.PI*2);ctxB.clip();
  fillExposed(ctxB,cx-R*sc,cy,R*diam*sc,R*sc);ctxB.restore();ctxB.restore();
  ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2.5;ctxB.beginPath();ctxB.arc(cx,cy,R*sc,0,Math.PI*2);ctxB.stroke();
  ctxB.strokeStyle='#155e27';ctxB.lineWidth=1.4;ctxB.setLineDash([4,3]);
  ctxB.beginPath();ctxB.arc(cx,cy,(R-cov)*sc,0,Math.PI*2);ctxB.stroke();ctxB.setLineDash([]);
  dimH(ctxB,cx-R*sc,cx+R*sc,cy+R*sc+4,`Ø${diam}`);
  const r=Math.max(3,db/20*sc*.52);
  for(let i=0;i<nb;i++){
    const ang=2*Math.PI*i/nb-Math.PI/2;
    S.barPositions.push({id:`B${i+1}`,label:`B${i+1}`,
      cx:cx+(R-cov)*sc*Math.cos(ang),cy:cy+(R-cov)*sc*Math.sin(ang),r,diam:db,type:'radial',found:S.barStatus[`B${i+1}`]||'unknown'});
  }
}

/* ── Viga ── */
function drawViga(p,W,H){
  const w=p.width||30,h=p.height||60,cov=p.cover||3;
  const nbb=p.bars_bottom_count||4,nbt=p.bars_top_count||2;
  const dbb=p.bars_bottom_diam||20,dbt=p.bars_top_diam||16,ds=p.stirrup_diam||8;
  const sc=Math.min(W*.48/w,H*.7/h);
  const ox=(W-w*sc)/2,oy=(H-h*sc)/2;
  fillConcrete(ctxB,ox,oy,w*sc,h*sc);
  // Zona picada derecha
  ctxB.save();ctxB.beginPath();ctxB.moveTo(ox+w*sc*.38,oy);ctxB.lineTo(ox+w*sc,oy);
  ctxB.lineTo(ox+w*sc,oy+h*sc);ctxB.lineTo(ox+w*sc*.38,oy+h*sc);ctxB.closePath();ctxB.clip();
  fillExposed(ctxB,ox,oy,w*sc,h*sc);ctxB.restore();
  ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2.5;ctxB.strokeRect(ox,oy,w*sc,h*sc);
  ctxB.strokeStyle='#155e27';ctxB.lineWidth=Math.max(1.2,ds/20*sc*.3);
  rrect(ctxB,ox+cov*sc,oy+cov*sc,w*sc-2*cov*sc,h*sc-2*cov*sc,3);
  dimH(ctxB,ox,ox+w*sc,oy-4,`${w}`);dimV(ctxB,oy,oy+h*sc,ox+w*sc+4,`${h}`);
  const spb=(w-2*cov)/(nbb-1||1),spt=(w-2*cov)/(nbt-1||1);
  for(let i=0;i<nbb;i++) S.barPositions.push({id:`B${i+1}`,label:`B${i+1}`,
    cx:ox+(cov+i*spb)*sc,cy:oy+(h-cov)*sc,r:Math.max(3,dbb/20*sc*.52),diam:dbb,type:'inferior',found:S.barStatus[`B${i+1}`]||'unknown'});
  for(let i=0;i<nbt;i++) S.barPositions.push({id:`T${i+1}`,label:`T${i+1}`,
    cx:ox+(cov+i*spt)*sc,cy:oy+cov*sc,r:Math.max(3,dbt/20*sc*.52),diam:dbt,type:'superior',found:S.barStatus[`T${i+1}`]||'unknown'});
}

/* ── Forjado ── */
function drawForjado(p,W,H){
  const th=p.thickness||25,nx=p.bars_x_count||10,cb=p.cover_bottom||3,ct=p.cover_top||3,dx=p.bars_x_diam||12,spx=p.bars_x_spacing||15;
  const WR=Math.min((nx-1)*spx+20+20,200);
  const sc=Math.min(W*.72/WR,H*.38/th);
  const ox=(W-WR*sc)/2,oy=(H-th*sc)/2;
  fillConcrete(ctxB,ox,oy,WR*sc,th*sc);
  ctxB.save();ctxB.beginPath();ctxB.rect(ox+WR*sc*.32,oy,WR*sc*.36,th*sc);ctxB.clip();
  fillExposed(ctxB,ox,oy,WR*sc,th*sc);ctxB.restore();
  ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2.5;ctxB.strokeRect(ox,oy,WR*sc,th*sc);
  dimH(ctxB,ox,ox+WR*sc,oy-4,`rep. ${WR.toFixed(0)}cm`);
  dimV(ctxB,oy,oy+th*sc,ox+WR*sc+4,`e=${th}`);
  const r=Math.max(3,dx/20*sc*.52);
  for(let i=0;i<nx;i++){
    const bx=ox+(10+i*spx)*sc;
    S.barPositions.push({id:`Xi${i+1}`,label:`Xi${i+1}`,cx:bx,cy:oy+(th-cb)*sc,r,diam:dx,type:'inferior',found:S.barStatus[`Xi${i+1}`]||'unknown'});
    S.barPositions.push({id:`Xs${i+1}`,label:`Xs${i+1}`,cx:bx,cy:oy+ct*sc,r,diam:dx,type:'superior',found:S.barStatus[`Xs${i+1}`]||'unknown'});
  }
}

/* ── Zapata ── */
function drawZapata(p,W,H){
  const L=p.length||200,WW=p.width||160,Hh=p.height||60,cb=p.cover_bottom||7,cs=p.cover_sides||7;
  const nx=p.bars_x_count||8,dx=p.bars_x_diam||16;
  const sc=Math.min(W*.62/L,H*.5/Hh);
  const ox=(W-L*sc)/2,oy=H*.62-Hh*sc;
  fillConcrete(ctxB,ox,oy,L*sc,Hh*sc);
  ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2.5;ctxB.strokeRect(ox,oy,L*sc,Hh*sc);
  dimH(ctxB,ox,ox+L*sc,oy+Hh*sc+4,`${L}`);dimV(ctxB,oy,oy+Hh*sc,ox+L*sc+4,`${Hh}`);
  const spx=(L-2*cs)/(nx-1||1),r=Math.max(3,dx/20*sc*.52);
  for(let i=0;i<nx;i++) S.barPositions.push({id:`X${i+1}`,label:`X${i+1}`,
    cx:ox+(cs+i*spx)*sc,cy:oy+(Hh-cb)*sc,r,diam:dx,type:'x',found:S.barStatus[`X${i+1}`]||'unknown'});
}

/* ── Escalera ── */
function drawEscalera(p,W,H){
  const riser=p.riser||17,tread=p.tread||28,n=Math.min(p.steps_count||5,7);
  const sc=Math.min(W*.65/(n*tread),H*.62/(n*riser+30));
  const ox=35,oy=H*.85;
  for(let i=0;i<n;i++){
    const px=ox+i*tread*sc,py=oy-i*riser*sc;
    fillConcrete(ctxB,px,py-riser*sc,tread*sc,riser*sc);
    ctxB.strokeStyle='#1a1a1a';ctxB.lineWidth=2;ctxB.strokeRect(px,py-riser*sc,tread*sc,riser*sc);
  }
  ctxB.fillStyle='#495057';ctxB.font=`500 10px ${mainFont}`;ctxB.textAlign='center';
  ctxB.fillText(`H=${riser}cm / A=${tread}cm`,ox+n*tread*sc/2,oy-n*riser*sc-14);
  // Barras longitudinales (una capa)
  const dl=p.bars_long_diam||12,sl=p.bars_long_sep||15;
  const nBars=Math.max(2,Math.floor(n*tread/sl)+1);
  for(let i=0;i<Math.min(nBars,8);i++) S.barPositions.push({
    id:`LL${i+1}`,label:`LL${i+1}`,cx:ox+i*sl*sc*0.95,cy:oy-i*riser*sc/n+2,r:Math.max(3,dl/20*sc*.5),diam:dl,type:'long',found:S.barStatus[`LL${i+1}`]||'unknown'});
}

/* ── Capa de barras (cvBars) ── */
function drawBarsLayer(){
  ctxBr.clearRect(0,0,cvBars.width,cvBars.height);
  S.barPositions.forEach(bar=>{
    const st=S.barStatus[bar.id]||'unknown';
    // Relleno según estado
    if(st==='found') ctxBr.fillStyle='rgba(21,128,61,0.9)';
    else if(st==='notfound') ctxBr.fillStyle='rgba(239,68,68,0.85)';
    else ctxBr.fillStyle='rgba(30,64,175,0.88)';
    ctxBr.beginPath();ctxBr.arc(bar.cx,bar.cy,bar.r,0,Math.PI*2);ctxBr.fill();
    ctxBr.strokeStyle='rgba(255,255,255,.85)';ctxBr.lineWidth=1.2;ctxBr.stroke();
    // Etiqueta
    ctxBr.fillStyle='#fff';
    ctxBr.font=`600 ${Math.max(7,Math.min(bar.r*.9,10))}px ${monoFont}`;
    ctxBr.textAlign='center';ctxBr.textBaseline='middle';
    ctxBr.fillText(bar.label,bar.cx,bar.cy);
    ctxBr.textBaseline='alphabetic';
  });
}

function updateBarStatusPanel(){
  const grid=document.getElementById('barStatusGrid');
  if(!S.barPositions.length){
    grid.innerHTML='<div style="font-size:.73rem;color:var(--g500);grid-column:1/-1;text-align:center;padding:.5rem">Aparecerán al configurar los parámetros de armadura</div>';
    document.getElementById('barSumTag').textContent='— barras';return;
  }
  document.getElementById('barSumTag').textContent=`${S.barPositions.length} barras`;
  grid.innerHTML=S.barPositions.map(bar=>{
    const st=S.barStatus[bar.id]||'unknown';
    const cls=st==='found'?'found':st==='notfound'?'notfound':'';
    const ico=st==='found'?'✓':st==='notfound'?'✗':'?';
    return`<div class="bar-stat ${cls}" onclick="toggleBar('${bar.id}')" title="Clic: cambiar estado">
      <div class="bs-label">${ico} ${bar.label}</div>
      <div class="bs-pos">Ø${bar.diam}mm</div>
    </div>`;
  }).join('');
}

function toggleBar(id){
  const cur=S.barStatus[id]||'unknown';
  S.barStatus[id]=cur==='unknown'?'found':cur==='found'?'notfound':'unknown';
  updateBarStatusPanel();drawBarsLayer();
}

/* ── Canvas helpers ── */
const mainFont="'Inter',sans-serif";
const monoFont="'IBM Plex Mono',monospace";

function rrect(ctx,x,y,w,h,r){
  ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.arcTo(x+w,y,x+w,y+r,r);
  ctx.lineTo(x+w,y+h-r);ctx.arcTo(x+w,y+h,x+w-r,y+h,r);ctx.lineTo(x+r,y+h);
  ctx.arcTo(x,y+h,x,y+h-r,r);ctx.lineTo(x,y+r);ctx.arcTo(x,y,x+r,y,r);
  ctx.closePath();ctx.stroke();
}
function dimH(ctx,x1,x2,y,lbl){
  ctx.strokeStyle='#868e96';ctx.lineWidth=.7;ctx.setLineDash([]);
  ctx.beginPath();ctx.moveTo(x1,y);ctx.lineTo(x2,y);ctx.stroke();
  ctx.beginPath();ctx.moveTo(x1,y-3);ctx.lineTo(x1,y+3);ctx.stroke();
  ctx.beginPath();ctx.moveTo(x2,y-3);ctx.lineTo(x2,y+3);ctx.stroke();
  ctx.fillStyle='#495057';ctx.font=`500 10px ${mainFont}`;ctx.textAlign='center';
  ctx.fillText(lbl,(x1+x2)/2,y-4);
}
function dimV(ctx,y1,y2,x,lbl){
  ctx.strokeStyle='#868e96';ctx.lineWidth=.7;
  ctx.beginPath();ctx.moveTo(x,y1);ctx.lineTo(x,y2);ctx.stroke();
  ctx.beginPath();ctx.moveTo(x-3,y1);ctx.lineTo(x+3,y1);ctx.stroke();
  ctx.beginPath();ctx.moveTo(x-3,y2);ctx.lineTo(x+3,y2);ctx.stroke();
  ctx.fillStyle='#495057';ctx.font=`500 10px ${mainFont}`;
  ctx.save();ctx.translate(x+4,(y1+y2)/2);ctx.rotate(Math.PI/2);
  ctx.textAlign='center';ctx.fillText(lbl,0,0);ctx.restore();
}

/* ── Herramientas dibujo (cvPick activo si tool=pick/erase; cvOver si tool=crack) ── */
function setTool(t){
  S.tool=t;
  document.querySelectorAll('.tool[id^="t-"]').forEach(b=>b.classList.remove('on'));
  document.getElementById('t-'+t)?.classList.add('on');
  // Canvas activo según herramienta
  cvPick.style.zIndex=t==='crack'?'1':'3';
  cvOver.style.zIndex=t==='crack'?'3':'1';
}
function setBrush(s){
  S.brush=s;
  document.querySelectorAll('.bs-dot').forEach(b=>b.classList.remove('on'));
  document.getElementById('b-'+(s<=8?'s':s<=18?'m':'l'))?.classList.add('on');
}

function cvPos(e){
  const el=S.tool==='crack'?cvOver:cvPick;
  const rect=el.getBoundingClientRect();
  const scX=el.width/rect.width,scY=el.height/rect.height;
  const src=e.touches?e.touches[0]:e;
  return{x:(src.clientX-rect.left)*scX,y:(src.clientY-rect.top)*scY};
}

function cvDown(e){
  S.drawing=true;
  const pos=cvPos(e);
  if(S.tool==='crack'){S.crackPts={x1:pos.x,y1:pos.y,x2:pos.x,y2:pos.y};}
  else{saveCV();paintAt(pos);}
}
function cvMove(e){
  if(!S.drawing)return;
  const pos=cvPos(e);
  if(S.tool==='crack'&&S.crackPts){S.crackPts.x2=pos.x;S.crackPts.y2=pos.y;drawCracks();}
  else paintAt(pos);
}
function cvUp(){
  if(S.tool==='crack'&&S.crackPts&&S.drawing){S.cracks.push({...S.crackPts});S.crackPts=null;drawCracks();updateCrackList();}
  S.drawing=false;
}
function cvTS(e){e.preventDefault();cvDown(e);}
function cvTM(e){e.preventDefault();cvMove(e);}

function paintAt({x,y}){
  const r=S.brush;
  if(S.tool==='pick'){
    ctxP.globalCompositeOperation='source-over';
    ctxP.beginPath();ctxP.arc(x,y,r,0,Math.PI*2);
    ctxP.fillStyle='rgba(251,146,60,.42)';ctxP.fill();
    ctxP.strokeStyle='rgba(234,88,12,.55)';ctxP.lineWidth=.7;ctxP.stroke();
  } else if(S.tool==='erase'){
    ctxP.globalCompositeOperation='destination-out';
    ctxP.beginPath();ctxP.arc(x,y,r*1.5,0,Math.PI*2);
    ctxP.fillStyle='rgba(0,0,0,1)';ctxP.fill();
    ctxP.globalCompositeOperation='source-over';
  }
}

function drawCracks(){
  ctxO.clearRect(0,0,cvOver.width,cvOver.height);
  S.cracks.forEach((c,i)=>{
    ctxO.strokeStyle='#dc2626';ctxO.lineWidth=1.8;ctxO.setLineDash([]);
    ctxO.beginPath();ctxO.moveTo(c.x1,c.y1);ctxO.lineTo(c.x2,c.y2);ctxO.stroke();
    ctxO.strokeStyle='#dc2626';ctxO.lineWidth=1;
    [-3,3].forEach(d=>{ctxO.beginPath();ctxO.moveTo(c.x1-d,c.y1-d);ctxO.lineTo(c.x1+d,c.y1+d);ctxO.stroke();});
    [-3,3].forEach(d=>{ctxO.beginPath();ctxO.moveTo(c.x2-d,c.y2-d);ctxO.lineTo(c.x2+d,c.y2+d);ctxO.stroke();});
  });
  if(S.crackPts){
    ctxO.strokeStyle='rgba(220,38,38,.65)';ctxO.lineWidth=1.5;ctxO.setLineDash([5,3]);
    ctxO.beginPath();ctxO.moveTo(S.crackPts.x1,S.crackPts.y1);ctxO.lineTo(S.crackPts.x2,S.crackPts.y2);ctxO.stroke();
    ctxO.setLineDash([]);
  }
}
function updateCrackList(){
  const panel=document.getElementById('crackPanel');
  const list=document.getElementById('crackList');
  if(!S.cracks.length){panel.style.display='none';return;}
  panel.style.display='block';
  list.innerHTML=S.cracks.map((c,i)=>{
    const lg=Math.sqrt((c.x2-c.x1)**2+(c.y2-c.y1)**2);
    return`<div class="crack-item">
      <svg width="13" height="13" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M3 2l2 4-3 2 4 4"/></svg>
      Fisura ${i+1} — ${lg.toFixed(0)}px aprox.
      <button class="crack-del" onclick="delCrack(${i})">×</button>
    </div>`;
  }).join('');
}
function delCrack(i){S.cracks.splice(i,1);drawCracks();updateCrackList();}
function saveCV(){if(S.cvHistory.length>20)S.cvHistory.shift();S.cvHistory.push(ctxP.getImageData(0,0,cvPick.width,cvPick.height));}
function undoCV(){if(S.cvHistory.length>0)ctxP.putImageData(S.cvHistory.pop(),0,0);}
function clearCV(){saveCV();ctxP.clearRect(0,0,cvPick.width,cvPick.height);S.cracks=[];drawCracks();updateCrackList();}

/* ══ FOTO ══ */
function loadPh(inp){const f=inp.files[0];if(!f)return;const r=new FileReader();r.onload=e=>{document.getElementById('phImg').src=e.target.result;document.getElementById('phPrev').style.display='block';document.getElementById('upz').style.display='none';};r.readAsDataURL(f);}
function clearPh(){document.getElementById('phPrev').style.display='none';document.getElementById('upz').style.display='block';document.getElementById('upInput').value='';}
function doDrop(e){e.preventDefault();e.currentTarget.classList.remove('drag');const f=e.dataTransfer.files[0];if(f){const dt=new DataTransfer();dt.items.add(f);document.getElementById('upInput').files=dt.files;loadPh(document.getElementById('upInput'));}}

/* ══ GENERAR DXF ══ */
async function generarDXF(){
  if(!S.struct){alert('Seleccione un tipo de estructura');return;}
  const p=getP();
  const def=STRUCTS[S.struct];
  const apiUrl=document.getElementById('apiUrl').value.replace(/\/$/,'');

  // Añadir estado de barras
  p.markers=S.barPositions.map(b=>({...b,found:S.barStatus[b.id]||'unknown'}));
  p.cracks_count=S.cracks.length;

  // Merge canvas para exportar
  const tmp=document.createElement('canvas');
  tmp.width=cvBase.width;tmp.height=cvBase.height;
  const ctx2=tmp.getContext('2d');
  ctx2.drawImage(cvBase,0,0);ctx2.drawImage(cvPick,0,0);ctx2.drawImage(cvBars,0,0);ctx2.drawImage(cvOver,0,0);
  p.canvas_data=tmp.toDataURL('image/png');

  const btn=document.getElementById('btnDXF');
  btn.disabled=true;setGenSt('spin','Generando plano DXF R2000…');setStep(4);

  try{
    const r=await fetch(`${apiUrl}${def.endpoint}`,{
      method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)
    });
    if(!r.ok){const e=await r.json().catch(()=>({detail:r.statusText}));throw new Error(e.detail||r.statusText);}
    const blob=await r.blob();
    const cd=r.headers.get('Content-Disposition')||'';
    const m=cd.match(/filename="([^"]+)"/);
    const fn=m?m[1]:`${S.struct}_${p.element_id||'E1'}.dxf`;
    const url=URL.createObjectURL(blob);
    const a=document.createElement('a');a.href=url;a.download=fn;a.click();URL.revokeObjectURL(url);
    setGenSt('ok',`✓ Descargado: ${fn}`);
    saveToHistory(p,def.label);
  }catch(e){
    setGenSt('err',`Error: ${e.message}`);
  }finally{btn.disabled=false;}
}

function setGenSt(st,msg){
  const d=document.getElementById('gdot'),t=document.getElementById('gstxt');
  d.className='sdot '+(st==='spin'?'spin':st==='ok'?'ok':st==='err'?'err':'idle');
  t.textContent=msg;
}
function setStep(n){
  for(let i=1;i<=4;i++){
    const el=document.getElementById('s'+i);
    el.classList.remove('on','done');
    if(i<n)el.classList.add('done');else if(i===n)el.classList.add('on');
  }
}

/* ══ HISTORIAL ══ */
function saveToHistory(p,label){
  if(!document.getElementById('cfg-hist')?.checked)return;
  const sec=S.struct==='pilar-rect'?`${p.width}×${p.depth}cm`:
    S.struct==='pilar-circ'?`Ø${p.diameter}cm`:
    S.struct==='viga'?`${p.width}×${p.height}cm`:
    S.struct==='escalera'?`${p.riser}×${p.tread}cm`:'—';
  const arm=p.bars_front_diam?`${p.bars_front_count}Ø${p.bars_front_diam}`:
    p.bars_diam?`${p.bars_count}Ø${p.bars_diam}`:'—';
  S.history.push({id:Date.now(),fecha:new Date().toLocaleDateString('es'),
    tipo:label,ref:p.element_id||'—',sec,arm,
    found:Object.values(S.barStatus).filter(v=>v==='found').length,
    fisuras:S.cracks.length,notes:p.notes||''});
  renderHistory();
}
function renderHistory(){
  const b=document.getElementById('histBody');
  if(!S.history.length){b.innerHTML='<tr><td colspan="8" style="text-align:center;color:var(--g500);padding:1.5rem">Sin registros</td></tr>';return;}
  b.innerHTML=S.history.map(r=>`<tr>
    <td class="mono">${r.ref}</td><td>${r.tipo}</td><td class="mono">${r.fecha}</td>
    <td class="mono">${r.sec}</td><td class="mono">${r.arm}</td>
    <td><span class="tag tg-green">${r.found} encontradas</span></td>
    <td>${r.fisuras>0?`<span class="tag" style="background:var(--r50);color:#991b1b;border-color:#fca5a5">${r.fisuras}</span>`:'—'}</td>
    <td><button class="btn bs" style="font-size:.67rem;padding:.2rem .45rem" onclick="delH(${r.id})">✕</button></td>
  </tr>`).join('');
  document.getElementById('totalReg').textContent=`${S.history.length} registros`;
  document.getElementById('hcount').textContent=S.history.length;
}
function delH(id){S.history=S.history.filter(r=>r.id!==id);renderHistory();}
function exportCSV(){
  if(!S.history.length)return;
  const cols=['Ref','Tipo','Fecha','Sección','Armadura','Barras encontradas','Fisuras','Notas'];
  const rows=S.history.map(r=>[r.ref,r.tipo,r.fecha,r.sec,r.arm,r.found,r.fisuras,r.notes||'']);
  const csv=[cols,...rows].map(r=>r.map(c=>`"${c}"`).join(',')).join('\n');
  const a=document.createElement('a');a.href='data:text/csv;charset=utf-8,'+encodeURIComponent(csv);a.download='inspecciones.csv';a.click();
}

/* ══ MISC ══ */
function saveObra(){alert('Datos de obra guardados. Se incluirán en el cajetín de todos los planos DXF.');}
function resetAll(){
  S.struct=null;S.barStatus={};S.cracks=[];S.crackPts=null;
  document.querySelectorAll('.sc').forEach(c=>c.classList.remove('on'));
  document.getElementById('fa').style.display='none';
  [ctxB,ctxP,ctxBr,ctxO].forEach(c=>{const cv=c.canvas;c.clearRect(0,0,cv.width,cv.height);});
  setStep(1);setGenSt('idle','Preparado para generar');drawCracks();updateCrackList();
}
async function checkApi(){
  const url=document.getElementById('apiUrl').value.replace(/\/$/,'');
  const dot=document.getElementById('apiDot'),txt=document.getElementById('apiTxt');
  dot.className='sdot spin';txt.textContent='Verificando…';
  try{const r=await fetch(`${url}/`,{signal:AbortSignal.timeout(4000)});
    if(r.ok){dot.className='sdot ok';txt.textContent='Conectado — backend activo';}
    else throw new Error(r.statusText);
  }catch(e){dot.className='sdot err';txt.textContent=`No disponible: ${e.message}`;}
}

/* Init */
window.addEventListener('DOMContentLoaded',()=>{
  document.getElementById('o-fecha').value=new Date().toISOString().split('T')[0];
  resizeCV();
  // Set initial z-index for canvas layers
  cvBase.style.zIndex='0';cvPick.style.zIndex='3';cvBars.style.zIndex='2';cvOver.style.zIndex='1';
});
window.addEventListener('resize',()=>{if(S.struct)renderCanvasTemplate();else resizeCV();});
</script>
</body>
</html>